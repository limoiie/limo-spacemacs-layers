(defconst limo-config-coding-copilot-enabled nil
  "Enable copilot or not.")

(defconst limo-config-coding-pyvenv-enabled nil
  "Enable pyvenv for letting conda manage the envs or not.")

(defconst limo-config-coding-pyvenv-default-env nil
  "Default conda env.
`nil' for nothing.")

(defconst limo-config-coding-pyvenv-workon-home nil
  "Work on home path.
`nil' for nothing.")

